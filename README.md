# AutoSign (CLI Branch)

这个分支是 AutoSign 的命令行版本开发分支。

- CLI 项目目录：[`CLI/`](CLI/)
- Web 版本目录：[`Web/`](Web/)

## CLI 快速开始

```bash
cd CLI
pip install -e .
autosign --help
```

详细命令与配置说明见：[`CLI/README.md`](CLI/README.md)
